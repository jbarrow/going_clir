#  Distant Supervision for Neural CLIR

## Installation

All of the code relies on the constant `root` set in `utils/constants.py`. This determines where the data and vectors will be stored.

## Running